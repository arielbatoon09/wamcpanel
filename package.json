{
  "name": "wamcpanel-root",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "install-all": "npm install --prefix backend && npm install --prefix frontend"
  }
}